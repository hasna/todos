# Stage B deferred: the former hosted container candidate workflow is
# intentionally quarantined until a separately reviewed authority stage exists.
version: 0.2

phases:
  build:
    commands:
      - echo 'Stage B deferred; no operational image workflow is available.'
      - exit 1
