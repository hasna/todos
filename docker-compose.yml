# Stage A containment-only evidence profile. This starts the dependency-light
# help command and exposes no port, datastore, provider, or remote runtime.
services:
  todos-containment:
    build: .
    command: ["bun", "dist/server/index.js", "--help"]
    restart: "no"
    profiles: ["stage-a-evidence"]
